pytorh官网计算机视觉的第二个教程
给出了两个迁移学习的实现方式:
* 微调网络参数，即加载预训练的模型后，再使用我们的数据进行训练，对应的训练脚本为ft_train.py
* 不改变预训练模型的卷积层，卷积层的工作的提取图片特征，我们认为这个预训练的模型提取特征的能力不错，我们使用一个新的全连接层接在卷积层后面，并训练这个全连接层的参数。

与官网教程相比，增加了保存模型参数的代码。
保存模型参数时，如果模型是在gpu上训练，同时保存前也没有转移到cpu上。
那么加载模型参数时，模型需要转移到gpu上。

经过对比，方法2（不改变卷积层）的训练效果更好（准确率更高）。